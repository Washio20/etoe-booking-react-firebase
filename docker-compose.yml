
services:
  app:
    build:
      context: .
      target: ${TARGET:-builder}
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge